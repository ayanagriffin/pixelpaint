/*global loadImage, createCanvas, resizeCanvas, background, text, Picture, windowWidth, windowHeight, image, round, floor, rect, fill, strokeWeight, Block*/

class ColorSquare{
  consructor(row, col, val){
    this.row = row;
    this.col = col;
    this.val = val;
    this.ra = 5;
  }
  
  
}